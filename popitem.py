dict1 = {"a": 1, "b": 1, "c": 3}
pop_item = dict1.popitem()
print(pop_item)

# popitem() removes and returns an arbitrary (key, value) pair from the dictionary.
# It deletes the the most recently added key-value pair. It returns the item as a tuple.

